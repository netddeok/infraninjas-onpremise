# my global config
global:
  scrape_interval: 15s
  evaluation_interval: 15s

## ktk ga ma juya ham 
alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

# jjs
  - job_name: 'node'
    static_configs:
      - targets: ['{{ node_exporter_target }}:9100']

# ktk
  - job_name : 'alertmanager'
    static_configs:
      - targets:
        - "localhost:9093"
