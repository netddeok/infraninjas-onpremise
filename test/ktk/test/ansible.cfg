[defaults]
inventory = ./inventory  # 인벤토리 파일 경로
remote_user = ansible  # 기본 접속 사용자
host_key_checking = False
ask_pass = True


[privilege_escalation]
become = true
become_method = sudo
become_user = root
